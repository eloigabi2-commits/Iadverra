DATABASE_URL="postgresql://postgres:postgres@localhost:5432/iadverra?schema=public"

# Segredo usado para assinar o cookie de sessão (JWT). Gere um valor aleatório
# forte em produção, por exemplo com: openssl rand -base64 32
JWT_SECRET="troque-este-valor-por-um-segredo-aleatorio"
